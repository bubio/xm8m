# RA状態機械置換前の回帰基準

- 日付: 2026-09-08
- 変更開始: `a8d73aa`、`codex/ra-hardcore-compliance-fixes`
- 媒体仕様: [45](45_RA統一媒体状態仕様と検証基準.md)、検証運用: [46](46_RA統一媒体動作確認運用手順.md)
- 次の内部設計: [47](47_RA媒体調停内部設計案.md)、[48](48_RA媒体状態機械詳細設計.md)

## 1. この基準の目的とブランチ境界

正常に使えている媒体操作を、本番処理を呼ぶ自動試験で保護してから内部設計を置き換える。
ユーザーの指示により、この基準をコミット・pushしたことを確認した後で別ブランチを作り、設計変更はそのブランチだけで行う。
元ブランチへ状態機械の置換を混ぜない。pushできない場合も、完了したと報告して先へ進まない。

## 2. 本番を通す試験境界

`Tests/ra_app_media_integration_test.cpp`は製品と同じ`App`、`DiskManager`、PC-8801MA VM、FDC、D88 parser、RA Library／MediaStore／Serviceをリンクする。
Appの公開メニュー入口、およびD&Dの媒体処理・reset完了処理を実際に呼ぶ。policyの結果だけを照合する試験ではない。

差し替えるのはEMU_SDLのホスト境界とRAのHTTP／認証保存だけである。SDLウィンドウを開かず、ユーザーのROM・設定・RA DB・Keychain・実ネットワークを使わない。
各実行で一意の一時ディレクトリを作り、生成D88と一時DBを置く。成功終了時にそのディレクトリだけを削除する。

本番の変更は以下のみ。媒体判定・処理順序は変更しない。

- Appへテスト接続用friendを一つ追加。
- `OnDropFile`の成功時reset処理を、その内容を保って`FinishDroppedDiskOpen`へ抽出。製品とテストが同じ処理を呼ぶ。

reset回数は実VMへ追加した試験用DEVICEで観測する。現VMは一回のresetで全DEVICEを二度resetするため、それを一回として数える。製品VMへ試験用カウンタを追加しない。

## 3. 固定した基本動作

| 条件・操作 | 観測する結果 |
|---|---|
| RA OFF、RA ONでログイン不能 | Drive 1／2の単独Open、他Drive維持、実FDCの挿入 |
| 複数D88 | 3種類の異なるhashの生成媒体を後続交換できる |
| 3bank D88 | bank 2の指定とbank切替。最初の2bankだけに限定しない |
| RA OFFのbank切替 | 現行の32フレームの交換待ち後に実FDCへ反映。即時mountを要求しない |
| 両Drive指定 | 選択したpath・bankを両Driveへ反映 |
| Eject | 各Driveの管理状態と実FDCの双方から取り出される |
| raw D88／bank指定M3UのD&D | 既存Drive割当、単一D88時のDrive 2 Eject、成功後のVM reset一回 |
| 両Driveのローカル検査失敗 | 一方のbank範囲不正で、旧両Drive構成・reset回数を維持 |
| RAの未登録応答 | Casual／Hardcoreの両方でOfflineとなり、Open・交換・Ejectを続けられる |
| Offline後に別Drive 1をmountして通常reset | 新しいStartingを経由し、登録応答でActiveになる。ロード完了時の追加VM resetなし |
| Activeの同一hash D&D | sessionを維持し、VM resetは一回 |

RA ONのpathは元D88でなく対応するworking copyであることも照合する。
RA OFFではRA Library・Service自体を用意しないため、それらの存在が通常操作の条件になっていないことを確認する。

## 4. 検証結果

```text
自動テスト:
- RA有効build: PASS
- scripts/verify_ra.sh --scope full: PASS（40件）
- 新規ra_app_media_integration_test: PASS
- git diff --check: PASS
- report: /tmp/xm8-pre-redesign-baseline-validation.md
- 実行時HEAD: a8d73aa＋この基準を構成する未コミット差分

ユーザーGUI受入:
- 今回: NOT RUN
- build: /Users/seiji/dev/_Emu/xm8mac/build-ra/xm8.app

ユーザー実受入:
- 実ネットワーク断・outbox: PENDING（今回の変更で実施していない）
```

この40件PASSを、48の状態機械の実装・全受入完了とは扱わない。D&DのOSイベント配送・LeaveMenu・画面描画は今回の新規試験の外側である。
RA待機中の全競合、Library START／CLIのApp経路、実frame評価、VM失敗注入とRA rollbackなどは、置換ブランチで本番経路試験を追加する。
未網羅の旧挙動を仕様として承認した意味ではない。期待値の基準は45とユーザーが確認した正常動作である。

## 5. 次の作業

1. この基準を含むcommitのremote到達を確認する。
2. 同じcommitから`codex/`接頭辞の別ブランチを作る。
3. 48の接続契約を本番経路試験で補い、ローカル準備・適用・復元から段階的に抽出する。
4. 構造変更と、再現した仕様不一致の修正を別commitにする。
5. 45にないmount拒否やreset条件を追加しない。各段階でこの基準と46の必要な検証を通す。

## 6. 設計変更ブランチでの進行記録

### 2026-09-08: 基準pushとローカル復元の抽出

- 基準commit: `94f13fcfb12af643f2f90b313f386199e81666f9`。
- `origin/codex/ra-hardcore-compliance-fixes`へのpush成功と同hashを確認後、`codex/ra-media-state-machine`を同commitから作成した。
- 設計変更ブランチで`DiskMountSnapshots`を追加。OpenDiskFromUser、OpenDiskSpecsFromMenu、OpenDroppedDisk、LaunchRaLibraryGameのsnapshot／restoreを共通化した。
- 呼出し元ごとの復元対象範囲と、復元失敗の既存扱いを保持する。RA判定・reset・非同期状態はまだ置き換えていない。
- 新規観測: Drive 1だけの復元がDrive 2の独立Ejectを取り消さないこと、Drive 1の復元不能でもDrive 2を復元すること、復元自体がresetしないこと。実DiskManager／FDCでPASS。
- `verify_ra.sh --scope full`: 40件PASS。report: `/tmp/xm8-media-snapshot-validation.md`。GUI・実ネットワークはNOT RUN／PENDINGのまま。

次の未完了工程は、Library START／CLIと非同期二Drive要求の本番試験追加、媒体要求の正規化、48の状態×イベント表に基づくCoordinatorへの置換である。
この抽出を状態機械全体の移行完了とは扱わない。次の担当者も、元ブランチへ戻して設計変更を混ぜず、この設計変更ブランチで継続する。


### 2026-09-08: 非同期本番試験とM3U D&Dの重複reset修正

変更開始: `b2fc5f0`、`codex/ra-media-state-machine`。状態機械の構造変更と分け、再現したreset不一致と回帰試験を一つの変更として扱う。

追加した本番経路の確認（すべてCasual／Hardcoreの双方）:

| 条件 | 観測結果 |
|---|---|
| ActiveのDrive 2に未検証の別D88を指定 | fake HTTP応答を保留した間は旧Drive構成・RA active hashを維持。成功後に対象Driveだけmount。resetなし |
| 上記の待機中にDrive 1のbank変更を要求 | busyとなり、両Driveを変更しない。元のDrive 2要求は成功応答で完了 |
| ActiveのDrive 1を3bank D88のbank 0からbank 2へ変更 | RA media change待機中は旧bank・旧active hashを維持。成功後に両者が変わる。Drive 2維持、resetなし |
| ActiveのDrive 2へ、このsessionで未検証のhashを指定し未登録応答 | Offlineへ移行して対象をmount。Drive 1を保持し、resetを追加しない |
| Readyから二Drive menu／CLI媒体適用／二行M3U D&D／raw複数bank D88 D&D | 登録応答ならActive、未登録応答ならOfflineで、要求された両Driveをmount。reset一回、transaction完了 |
| Readyから一行M3U D&D | 登録／未登録の双方でDrive 1をmount、Drive 2は空、reset一回 |
| 上記登録ゲームのLibrary START | 実際に保存されたprofileを使い、同じactive hashでもStartingから新しい起動を行う。両Drive構成を再現し、reset一回 |

再現した問題と修正:

- 修正前の本番Appでは、Readyから二行M3UをD&Dして未登録応答を返す試験で、VM reset回数が期待1に対し実測2となった。
- `OpenDroppedDisk`がM3Uに限り`OpenDiskFromUser`の`reset_after_commit`へfalseを渡し、Drive 1識別開始時のresetと、後段のD&D／pair完了時のresetが重なっていた。
- D&Dは必ずresetを伴うというユーザー確認済みの前提に従い、raw D88と同様にM3Uもtrueを渡す。同期で開始するanchorは先にresetせず、既存の完了処理が一回だけ行う。
- 変更箇所はこのreset担当の伝達のみ。媒体の許可条件・RA応答分類・Drive割当を変更していない。
- 修正後、登録／未登録、一行／二行M3U、raw D88、および既存回帰がPASS。

```text
自動テスト:
- RA有効build: PASS
- verify_ra.sh --scope full: PASS（40件。既存App統合test内のシナリオを増やしたためCTest件数は不変）
- git diff --check: PASS
- report: /tmp/xm8-async-media-validation.md
- 実行時HEAD: b2fc5f0＋今回のコード・テスト差分

ユーザーGUI受入:
- GU-04（M3U D&D／Library）: NOT RUN
- build: /Users/seiji/dev/_Emu/xm8mac/build-ra/xm8.app

ユーザー実受入:
- 実ネットワーク断・outbox: PENDING
```

試験境界と残件:

- CLIはparse済み`DiskSpec`を受ける本番`OpenStartupDisks`から実行する。process起動・引数parse・起動全体はこの試験の対象外。LibraryもSTARTの本番処理を呼び、画面上の入力は対象外。
- 新規二Drive起動の確認は最終mount・session・resetを対象とし、全段階の原子的commitを保証する試験ではない。Activeから任意の別D88二枚へ同時交換するケース、Drive 2のみ異なるRA Game IDを返すケース、通信断、VM失敗後のRA rollbackは引き続き本番経路の追加対象。
- fakeの成功応答はGame ID 1234に統一している。別Game ID／transport error／応答順序逆転まで確認したとは扱わない。
- 次工程は48のMediaRequestへの入口正規化とCoordinator接続。その前後で、この試験を必ず維持する。今回の局所修正を状態機械置換完了とは扱わない。


### 2026-09-08: 非同期anchorのVM適用・復元を共通化

前段の試験追加・M3U修正は`bce49caf2154c4e2f2cd7804b864318e4cd0127a`として設計変更ブランチへpush済み。そのcommitから、次の構造変更を分離した。

- `Source/UI/diskmounttargets.h`に`DiskMountTargets`を追加。DriveごとのKeep／Eject／Mount(path, bank)を値として保持し、Drive 1 → Drive 2の順に適用する。Keepはsnapshot／復元対象からも外す。
- これは48のPreparedMediaが持つVM適用先の部分であり、requestId・起動意図・reset・RA hashを持つ完成したMediaRequest／Coordinatorではない。ローカル検査、RA、profile保存、resetは呼出し側の責務を維持する。
- `BeginRaMediaChange`で既存の単独／raw pair指定を一度だけ適用先へ変換し、対象Driveのsnapshotを保持する。非同期状態に`open_pair`／`target_banks`／Drive 1用の個別snapshotを重ねて持たない。
- `ProcessRaMediaChange`の成功とRA拒否後のローカルmountが同じ`Apply`を使う。Drive 2検証失敗とDrive 1変更失敗のOffline処理を`CommitRaMediaChangeOffline`へ共通化。RA終了が旧transactionを消すため、その直前に要求を値で退避する。
- VM失敗後は同じ対象snapshotを復元し、既存のRA rollbackへ進む。RA OFF／非対応ゲームの許可条件、必要なreset、profile保存の必須／通知の区別は変更しない。失敗通知の表記は共通の「VM rejected … media」にまとめた。
- Library STARTの同期mountも同じ`Apply`を利用する。Libraryの検証・非同期起動・保存・resetの順序は維持する。

追加した観測:

1. Activeの3bank D88からraw D&Dでbank 0/1へ交換。Drive 2検証待機中に旧Drive・RA hash・reset回数を維持し、成功でも未登録応答でも要求した二Driveをmount、reset一回（Hardcoreで本番Appを実行）。
2. 同一RAゲームへ登録された別D88へのDrive 1変更について、準備後にテスト専用working copyを削除しVM失敗を注入。旧DriveとRA hashを復元し、待機中に独立EjectしたDrive 2は再挿入しない。resetなし、transaction完了（Hardcore）。
3. RA OFFの実DiskManager／FDCで、別ファイル＋任意bankの二Drive適用、Drive 2適用失敗後の両Drive復元、Drive 2だけのEjectとDrive 1 Keepを確認。アダプタ自身のresetは0回。

```text
自動テスト:
- RA有効build: PASS
- verify_ra.sh --scope full: PASS（40件）
- git diff --check: PASS
- report: /tmp/xm8-mount-targets-validation.md
- 実行時HEAD: bce49ca＋今回の構造変更・テスト差分
- 48生成定義の --check --self-test: PASS

ユーザーGUI受入:
- 今回: NOT RUN
- build: /Users/seiji/dev/_Emu/xm8mac/build-ra/xm8.app

ユーザー実受入:
- 実ネットワーク断・outbox: PENDING
```

残件を更新する。VM失敗注入と成功rollbackの本番経路を一件確認したが、各Drive・各RA失敗種別・rollback不能の全組合せは未網羅。Libraryの非同期側、Auxiliary側、単独Openの同期処理はまだ旧適用処理を持つ。
次工程はこれらのPrepare／Apply接続と、任意の二Drive要求を受付時点で固定する入口正規化。`DiskMountTargets`が任意の二Driveを表現できることと、全入口が一要求に統一されたことは区別する。48の状態×イベントCoordinator本体への置換は引き続き未完了。

### 2026-09-08: AuxiliaryとLibrary非同期側の適用先を共通化

`5948735`（push済み）から続行。サブエージェントは使用していない。

- `BeginRaAuxiliaryValidation`がDrive 2の適用先を`DiskMountTargets`へ格納し、raw pair／Libraryの呼出し元がDrive 1を追加する。成功時と未登録anchorのOffline fallbackが同じ適用先を使う。
- `RaDiskTransaction`から重複する`anchor_target`、`has_anchor_target`、旧Drive構成の6フィールドを削除。anchor変更の有無は適用先から導出し、旧構成は`DiskMountSnapshots`だけに保持する。
- RA応答の分類、検証開始順序、profile保存、resetの担当は維持。RA OFF／非対応媒体の許可条件を追加していない。
- 復元範囲の変更は分離した。旧Auxiliaryは通常のVM失敗では両Driveを復元する一方、未登録anchorのfallback失敗ではanchor変更がない場合にDrive 2だけを復元する。この差を保つため、後者の単独Drive復元はまだ明示処理を残す。全経路のKeep除外が完了したとは扱わない。
- 前段で追加した本番App統合試験（単独Drive 2検証、登録／未登録のraw・M3U・CLI・menu起動、Library非同期起動、reset回数）を含む全40テストがPASS。今回は新しい失敗注入ケースは追加していないため、AuxiliaryのVM／profile失敗の全分岐を実測済みとは扱わない。

```text
自動テスト:
- RA有効build: PASS
- verify_ra.sh --scope full: PASS（40件）
- report: /tmp/xm8-auxiliary-targets-validation.md
- 実行時HEAD: 5948735＋今回の構造変更
- git diff --check: PASS

ユーザーGUI受入:
- GU-04: NOT RUN
- build: /Users/seiji/dev/_Emu/xm8mac/build-ra/xm8.app

ユーザー実受入:
- 実ネットワーク断・outbox: PENDING
```

次工程: 単独Openの同期適用と、任意の二Drive要求の受付時点での正規化が残る。二Driveを順番に別要求として扱ってしまう既存入口について、本番App試験で待機中・完了時の構成を観測してから一要求へ接続する。その後48の状態×イベントCoordinatorへ移す。今回も状態機械本体の置換完了ではない。

### 2026-09-08: Activeの二ファイル交換を一つのanchor transactionへ接続

`f876fe8`（push済み）から続行。サブエージェントは使用していない。

再現と修正:

- 本番App統合試験で、Activeの3bank D88 bank 0から「同D88 bank 2をDrive 1、別D88 bank 0をDrive 2」へmenu二Drive指定すると、`RA Drive 1 operation is already pending`でfalseを返すことを再現した。Drive 1の非同期変更開始後にDrive 2を独立要求として送っていた。
- `TryBeginRaPairedAnchorChange`で、二Driveのローカルprobe後、Drive 1が`ChangeAnchorMedia`の場合に両Driveの作業コピーと適用先を確定する。Drive 2を後から別要求として送らない。
- `BeginRaMediaChangeTargets`が任意のpath／bankを持つ適用先を受け、Drive 2の選択bankを検証してからDrive 1のRA media changeへ進む。既存raw pair／単独bank交換もこの処理を使い、旧引数からの変換はwrapperに置いた。
- menu二Drive指定（CLIの委譲先を含む）と二行M3U D&Dを接続。成功・Offline fallback・VM復元は前段で共通化した処理を使用する。D&Dはreset要求を保持し、通常の同一ゲーム交換にはresetを追加しない。

確認した本番Appシナリオ:

- Casual／Hardcore × menu／M3U D&D × RA検証成功／未登録応答の8通り。待機中は旧Drive構成、RA active hash、reset回数を維持。完了時は要求した二Driveをmountしtransaction終了、成功はActive、未登録応答はOffline。D&Dのみreset一回。
- 同じfixtureでDrive 2のbank 99を先に指定し、拒否時にtransactionを開始せず旧Drive構成・hash・reset回数を維持することを確認。
- 修正前に新しいmenuケースがFAIL、修正後に全40テストがPASS。既存のRA OFF、未登録ゲーム起動、Library、VM失敗／rollback試験も実行した。

```text
自動テスト:
- RA有効build: PASS
- verify_ra.sh --scope full: PASS（40件）
- report: /tmp/xm8-paired-anchor-validation.md
- 実行時HEAD: f876fe8＋今回のコード・テスト差分
- git diff --check: PASS

ユーザーGUI受入:
- GU-04: NOT RUN
- build: /Users/seiji/dev/_Emu/xm8mac/build-ra/xm8.app

ユーザー実受入:
- 実ネットワーク断・outbox: PENDING
```

適用範囲と次工程:

- 今回の入口接続は、既存policyがDrive 1を`ChangeAnchorMedia`に分類する二Drive交換に限定する。`TryBegin...`は移行用であり、48の完成したCoordinatorではない。新規／別ゲーム開始、同一hash、Offline、RA OFFは既存経路を維持した。一行M3UのEjectも今回の接続対象外。
- 新しい試験のRA成功応答は同じGame ID、未登録応答は一律未登録。Drive 1だけ登録・Drive 2だけ別Game ID、通信断、今回の二ファイル経路固有のVM失敗はまだ追加対象。
- 次は上記の残るsession分類でも受付時点の要求を固定し、適用・reset担当を統一する。ローカルGame IDとRA Game IDの区別、Offline後の再識別と補助媒体再検証、競合Ejectの範囲を48の表へ接続する必要がある。状態機械本体の置換は引き続き未完了。

### 2026-09-08: 一行M3UのEjectもanchor交換要求へ保持

`7c9acde`（push済み）から続行。サブエージェントは使用していない。

- 本番Appで、Active中に同一ゲームの検証済み別hashを一行M3UでD&Dすると、Drive 1変更がPendingの間にDrive 2だけ先にEjectされることを再現した。
- `TryBeginRaPairedAnchorChange`にDrive 2を閉じる意図を渡し、一行M3UではDrive 1 Mount＋Drive 2 Ejectを同じ`DiskMountTargets`に保持する。menuの`close_drive2`も同じ接続を使う。既存のChangeAnchorMedia以外の分類は今回変更しない。
- Casual／Hardcoreの本番App試験で、待機中は両Drive・active hash・reset回数を維持し、完了時に指定anchor＋Drive 2空、Active維持、reset一回を確認した。修正前は待機中のDrive 2挿入確認がFAIL、修正後はPASS。
- このfixtureは事前にDrive 2でhashを検証して同一ゲームへ関連付けている。fake HTTPの未登録応答へ切り替えるだけでは、検証済みhashのRA変更を拒否できず、Offline期待の試験は失敗した。試験を実際の成功経路に限定し、今回の一行M3UについてRA拒否を注入済みとは扱わない。

```text
自動テスト:
- RA有効build: PASS
- verify_ra.sh --scope full: PASS（40件）
- report: /tmp/xm8-paired-eject-validation.md
- 実行時HEAD: 7c9acde＋今回のコード・テスト差分
- git diff --check: PASS

ユーザーGUI受入:
- GU-04: NOT RUN
- build: /Users/seiji/dev/_Emu/xm8mac/build-ra/xm8.app

ユーザー実受入:
- 実ネットワーク断・outbox: PENDING
```

残件: 同一hash／新規・別ゲーム／Offlineの全入口正規化、同じ要求が変更するDriveへの競合Ejectの整理、状態×イベントCoordinator接続。一行M3U固有のRA失敗・VM失敗注入も未実施。今回の修正を状態機械置換完了とは扱わない。

### 2026-09-08: 競合Ejectの受付を要求のDrive範囲へ合わせる

`da7797c`（push済み）から続行。サブエージェントは使用していない。

- 48 §4.2に従い、`CanEjectRaMedia`へ補助Drive変更のPendingを明示的に渡す。AppはAuxiliary検証または要求の`mount_targets.Changes(1)`から導出する。二Drive要求中のEjectをBusyにし、Drive 1単独待機中のDrive 2 Ejectは維持する。
- 本番App試験で二ファイル交換中のDrive 2 Ejectが許可されることを修正前に再現。修正後はCasual／Hardcore、menu／M3U、成功／未登録応答の既存二Driveケース、および一行M3UのPending中に競合Ejectを拒否し、主要求の最終mount・resetを維持した。
- 独立Ejectの既存本番試験もPASS。Drive 1単独変更中にDrive 2をEjectし、その後VM失敗とRA rollbackが起きてもDrive 2を再挿入しない。
- RA OFFとOfflineの通常Ejectは既存試験でPASS。新規RA照会、reset、媒体の許可条件は追加していない。独立EjectのrequestId／完了通知はCoordinator接続時の残件。

```text
自動テスト:
- RA有効build: PASS
- verify_ra.sh --scope full: PASS（40件）
- report: /tmp/xm8-eject-ownership-validation.md
- 実行時HEAD: da7797c＋今回のコード・テスト差分
- git diff --check: PASS

ユーザーGUI受入:
- GU-04: NOT RUN
- build: /Users/seiji/dev/_Emu/xm8mac/build-ra/xm8.app

ユーザー実受入:
- 実ネットワーク断・outbox: PENDING
```
